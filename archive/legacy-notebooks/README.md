# Legacy Notebooks

Previous comprehensive/operator notebooks retained as historical reference.

Current operator actions should go through `just` recipes and current docs:

```bash
just check-env
just edge-status
just arm-test
```

For the current training flow, use:

```text
docs/lerobot-training-session.md
```
