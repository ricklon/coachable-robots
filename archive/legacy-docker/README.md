# Legacy Dockerfiles

Previous Dockerfile variants kept for reference.

Current image definitions live under `channels/`, and current arm image builds
go through:

```bash
just build-arm
just push-arm
```

Do not use these legacy Dockerfiles for the CHI@Edge SO-ARM path unless you are
recovering old behavior intentionally.
